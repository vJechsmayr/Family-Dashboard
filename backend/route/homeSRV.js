module.exports = {
    setup: function(app, db, sendResponse){
        // app.get('/home/layout', (req, res) =>{
        //     const origin = '/home/layout';
        //     let query = 'SELECT * FROM layouts';

        //     db.query(query, (err, result, fields)=>{
        //         if(err){
        //             sendResponse(origin, res, false, 'Fehler beim Abfragen der Layout-Daten! \n' + err + '\n' + err.message + '\n' + query);
        //         }else{
        //             sendResponse(origin, res, true, '', result);
        //         }
        //     });
        // });


    }
};